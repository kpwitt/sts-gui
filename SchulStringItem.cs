using Avalonia.Controls.Selection;

namespace StS_GUI_Avalonia
{
    public class SchulStringItem
{

        void SelectionChanged(object sender, SelectionModelSelectionChangedEventArgs e)
        {
            // ... handle selection changed
        }
       // public SchulStringItem()
       // {
       //     //Items = CreateItems();​
       // // SelectionModel.Source can be set to Items here, or if it is left null it will be set by
       // // the `ListBox` when bound.
       // //Selection = new SelectionModel();
       //  //   Selection.SelectionChanged += SelectionChanged;​
       // // Select item 10 in Items.
       //// Selection.Select(10);
       // }​
    //public ObservableCollection<SchulStringItem> Items { get; }
    //    public SelectionModel Selection { get; }
    }
}
